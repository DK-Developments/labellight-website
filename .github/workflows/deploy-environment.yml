name: Deployment variables

on:
  workflow_call:
    inputs:
      environment:
        description: 'Environment to deploy to (dev or prod)'
        required: true
        type: string
      environment_display_name:
        description: 'Display name for the environment'
        required: true
        type: string

jobs:
  deploy:
    name: Deploy to ${{ inputs.environment_display_name }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-southeast-2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.0
          terraform_wrapper: false

      - name: Terraform Init
        run: |
          terraform init -reconfigure \
            -backend-config="bucket=printerapp-terraform-state-${{ inputs.environment }}" \
            -backend-config="dynamodb_table=printerapp-terraform-locks-${{ inputs.environment }}"
        working-directory: ./terraform

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build Lambda Layer for Stripe
        run: |
          rm -rf ./terraform/stripe_layer/python
          mkdir -p ./terraform/stripe_layer/python
          pip install -r ./terraform/stripe_layer/requirements.txt -t ./terraform/stripe_layer/python --quiet

      - name: Terraform Plan
        run: |
          VAR_FILE=""
          if [ "${{ inputs.environment }}" = "prod" ]; then
            VAR_FILE="-var-file=prod.tfvars"
          fi
          terraform plan $VAR_FILE -out=tfplan
        working-directory: ./terraform
        env:
          TF_VAR_google_client_id: ${{ secrets.TF_VAR_GOOGLE_CLIENT_ID }}
          TF_VAR_google_client_secret: ${{ secrets.TF_VAR_GOOGLE_CLIENT_SECRET }}
          TF_VAR_stripe_secret_key: ${{ secrets.STRIPE_SECRET_KEY }}
          TF_VAR_stripe_webhook_secret: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
          TF_VAR_environment: ${{ inputs.environment }}

      - name: Show bucket policy diff
        run: terraform show -no-color tfplan | sed -n '/aws_s3_bucket_policy.website/,/}/p'
        working-directory: ./terraform

      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan
        working-directory: ./terraform
        env:
          TF_VAR_google_client_id: ${{ secrets.TF_VAR_GOOGLE_CLIENT_ID }}
          TF_VAR_google_client_secret: ${{ secrets.TF_VAR_GOOGLE_CLIENT_SECRET }}
          TF_VAR_stripe_secret_key: ${{ secrets.STRIPE_SECRET_KEY }}
          TF_VAR_stripe_webhook_secret: ${{ secrets.STRIPE_WEBHOOK_SECRET }}

      - name: Get Terraform Outputs
        id: tf_outputs
        run: |
          echo "bucket_name=$(terraform output -raw website_bucket_name)" >> $GITHUB_OUTPUT
          echo "cloudfront_id=$(terraform output -raw cloudfront_distribution_id)" >> $GITHUB_OUTPUT
          echo "website_url=$(terraform output -raw website_url)" >> $GITHUB_OUTPUT
          echo "cognito_domain=$(terraform output -raw cognito_domain)" >> $GITHUB_OUTPUT
          echo "cognito_client_id=$(terraform output -raw cognito_client_id)" >> $GITHUB_OUTPUT
          echo "api_url=$(terraform output -raw api_url)" >> $GITHUB_OUTPUT
        working-directory: ./terraform

      - name: Generate config.js
        run: |
          cat > ./src/website/js/config.js << EOF
          const CONFIG = {
            COGNITO_DOMAIN: '${{ steps.tf_outputs.outputs.cognito_domain }}',
            CLIENT_ID: '${{ steps.tf_outputs.outputs.cognito_client_id }}',
            API_URL: '${{ steps.tf_outputs.outputs.api_url }}'
          };
          EOF

      - name: Fetch Stripe prices and generate stripe-pricing.js
        env:
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
        run: |
          # Debug: Check if STRIPE_SECRET_KEY is set (show first 10 chars only for security)
          if [ -z "$STRIPE_SECRET_KEY" ]; then
            echo "❌ ERROR: STRIPE_SECRET_KEY is empty or not set!"
            echo "Please ensure the secret is configured in GitHub Environment: ${{ inputs.environment }}"
            exit 1
          else
            echo "✅ STRIPE_SECRET_KEY is set (starts with: ${STRIPE_SECRET_KEY:0:10}...)"
          fi
          
          # Fetch prices from Stripe with error handling
          HTTP_RESPONSE=$(curl -s -w "\n%{http_code}" -u "$STRIPE_SECRET_KEY:" \
            "https://api.stripe.com/v1/prices?active=true&expand[]=data.product&limit=100")
          
          HTTP_CODE=$(echo "$HTTP_RESPONSE" | tail -n1)
          PRICES=$(echo "$HTTP_RESPONSE" | sed '$d')
          
          echo "Stripe API HTTP Status: $HTTP_CODE"
          
          if [ "$HTTP_CODE" != "200" ]; then
            echo "❌ ERROR: Stripe API returned status $HTTP_CODE"
            echo "Response: $PRICES"
            exit 1
          fi
          
          # Check if we got valid JSON with data
          PRICE_COUNT=$(echo "$PRICES" | python3 -c "import sys,json; d=json.load(sys.stdin); print(len(d.get('data',[])))" 2>/dev/null || echo "0")
          echo "Found $PRICE_COUNT prices from Stripe API"
          
          export PRICES
          
          python3 << 'PYTHON_SCRIPT'
          import json
          import os
          import sys

          prices_json = os.environ.get('PRICES', '{}')
          try:
            prices_data = json.loads(prices_json)
          except json.JSONDecodeError as e:
            print(f"❌ ERROR: Failed to parse Stripe response as JSON: {e}")
            sys.exit(1)
          
          print(f"Processing {len(prices_data.get('data', []))} prices from Stripe...")

          plans = {}
          skipped_no_plan_key = 0
          for price in prices_data.get('data', []):
            product = price.get('product', {})
            metadata = price.get('metadata', {})
            product_metadata = product.get('metadata', {}) if isinstance(product, dict) else {}
            
            plan_key = metadata.get('plan_key') or product_metadata.get('plan_key')
            if not plan_key:
              product_name = product.get('name', 'Unknown') if isinstance(product, dict) else 'Unknown'
              print(f"  ⚠️  Skipping price {price.get('id')} (product: {product_name}) - no plan_key metadata")
              skipped_no_plan_key += 1
              continue
            
            interval = price.get('recurring', {}).get('interval', 'month')
            unit_amount = price.get('unit_amount', 0) / 100
            
            plan_name = f"{plan_key}_{interval}"
            print(f"  ✅ Found plan: {plan_name} (price: ${unit_amount})")
            
            plans[plan_name] = {
              'priceId': price.get('id'),
              'productId': product.get('id') if isinstance(product, dict) else product,
              'name': product.get('name', '') if isinstance(product, dict) else '',
              'price': unit_amount,
              'interval': interval,
              'currency': price.get('currency', 'usd').upper()
            }

          config = {
            'currency': {'symbol': '$', 'code': 'USD'},
            'trial': {'days': 7, 'requiresCard': True},
            'guarantee': {'days': 30},
            'plans': plans
          }

          js_content = f'''/**
           * Stripe Pricing Configuration
           * Auto-generated during deployment - DO NOT EDIT MANUALLY
           * 
           * @author Eprouvez
           * @version 1.0.0
           */
          var PricingConfig = {json.dumps(config, indent=2)};

          PricingConfig.formatPrice = function(price) {{
            return this.currency.symbol + price.toFixed(2);
          }};

          window.PricingConfig = PricingConfig;
          '''

          with open('./src/website/js/stripe-pricing.js', 'w') as f:
            f.write(js_content)

          print(f"\n📊 Summary:")
          print(f"   Total prices from Stripe: {len(prices_data.get('data', []))}")
          print(f"   Skipped (no plan_key): {skipped_no_plan_key}")
          print(f"   Plans configured: {len(plans)}")
          
          if len(plans) == 0:
            print("\n⚠️  WARNING: No plans were generated!")
            print("   Make sure your Stripe products have 'plan_key' in their metadata.")
            print("   Expected values: 'single', 'team', 'business'")
          
          print(f"\n✅ Generated stripe-pricing.js with {len(plans)} plans")
          PYTHON_SCRIPT

      - name: Generate jwt-utils.js
        run: |
          cat > ./src/website/js/jwt-utils.js << 'EOF'
          // JWT utility functions for token validation and decoding

          /**
           * Decode a JWT token and return the payload
           * @param {string} token - The JWT token to decode
           * @returns {object|null} - The decoded payload or null if invalid
           */
          function decodeJwtToken(token) {
            if (!token) return null;
            
            try {
              const parts = token.split('.');
              if (parts.length !== 3) return null;
              
              const payload = parts[1];
              const decoded = atob(payload.replace(/-/g, '+').replace(/_/g, '/'));
              return JSON.parse(decoded);
            } catch (e) {
              console.error('Failed to decode JWT token:', e);
              return null;
            }
          }

          /**
           * Check if a JWT token is valid and not expired
           * @param {string} token - The JWT token to validate
           * @returns {boolean} - True if token is valid and not expired
           */
          function isTokenValid(token) {
            if (!token) return false;
            
            try {
              const payload = decodeJwtToken(token);
              if (!payload) return false;
              
              // Check expiration
              const exp = payload.exp;
              if (!exp) return false;
              
              const now = Math.floor(Date.now() / 1000);
              return exp > now;
            } catch (e) {
              console.error('Failed to validate JWT token:', e);
              return false;
            }
          }
          EOF

      - name: Sync website files to S3
        run: |
          aws s3 sync ./src/website/ s3://${{ steps.tf_outputs.outputs.bucket_name }}/ \
            --delete \
            --exclude ".git/*" \
            --exclude ".github/*"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ steps.tf_outputs.outputs.cloudfront_id }} \
            --paths "/*"

      - name: Display deployment info
        run: |
          echo "✅ Successfully deployed to ${{ inputs.environment_display_name }}"
          echo "🌐 Website URL: ${{ steps.tf_outputs.outputs.website_url }}"

